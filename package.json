{
  "name": "study-hub",
  "version": "1.0.0",
  "description": "A comprehensive educational platform for all subjects.",
  "main": "server/index.js",
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build && cd .. && rm -rf server/public && mkdir -p server/public && cp -R client/dist/. server/public/ && npx prisma generate && npx prisma migrate deploy && npm run seed || true",
    "start": "node server/index.js",
    "seed": "npx prisma db seed",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.5",
    "nodemon": "^3.0.1",
    "prisma": "5.22.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "keywords": [
    "education",
    "lms",
    "study-hub",
    "react",
    "node",
    "prisma",
    "postgresql"
  ],
  "author": "Antigravity",
  "license": "MIT"
}